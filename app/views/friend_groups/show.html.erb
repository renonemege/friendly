      <div class="border">
      <div class="card-product " style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path(@friend_group.photo.key, :crop => :fill) %> ')">
        <div class="card-product-infos">
          <h3><%= @friend_group.title %></h3>
          <p>--<%= @friend_group.description %>--</p>
          <p>Adress: <%= @friend_group.address%></p>
          <p>Price: <%= @friend_group.price %>€</p>
        </div>
      </div>
    </div>

  <% if current_user && @friend_group.user%>
    <li class= " btn btn-warning ml-2">
       <%= link_to "delete group ", friend_group_path(@friend_group), method: :delete, data: { confirm: "Are you sure?"}, class: "a" %>
    </li>
    <li class= " btn btn-warning ml-2">
      <%= link_to 'Edit', edit_friend_group_path(@friend_group), class: "a" %>
    </li>

  <% end %>
    <li class= " btn btn-warning ml-2">
      <%= link_to 'Back', friend_groups_path, class: "a" %>
    </li>



<div class="container mt-5`">
  <div class="row justify-content-center">
    <div class="col-6">
      <h2>Add a booking</h2>
      <%= simple_form_for [@friend_group, @booking] do |f| %>
        <div class="form-inputs">
          <%= f.input :start_date, as: :string, input_html: { class: "date start" } %>
          <%= f.input :end_date, as: :string, input_html: { class: "date end" } %>
          <%= f.hidden_field :status, value: 'available' %>
          <%= f.input :total_price, input_html: {value: f.object.total_price || '0', class: "total-price"} %>
        </div>
        <div class="form-actions btn btn-outline-warning">
          <%= f.button :submit%>
        </div>
      <% end %>

    </div>
  </div>
</div>

<ul class="list-group">
  <% @friend_group.bookings.each do |booking| %>
    <div class="review mb-5 mt-3">
      <li class="list-group-item">
        <%= (booking.end_date - booking.start_date).to_i %>
        <p><%= booking.end_date%></p>
        <p><%= booking.total_price%></p>
      </li>
    </div>
  <% end %>
</ul>

<% if current_user %>
  <div class="container mt-100 p-100`">
    <h2>Add a review</h2>
    <%= simple_form_for [@friend_group, @review] do |f| %>
      <div class="form-inputs">
        <%= f.input :content %>
        <%= f.input :stars %>
      </div>
      <div class="form-actions">
        <%= f.button :submit %>
      </div>
    <% end %>
  </div>
<% end %>



<ul class="list-group">
    <% if @friend_group.reviews.nil? %>
     <% 5.times do  %>
        <span class="far fa-star"></span>
     <% end %>
    <% else %>
      <% @friend_group.reviews.each do |review| %>
        <div class="review mb-5 mt-3">
          <li class="list-group-item">
            <% review.stars.times do  %>
              <span class="fas fa-star"></span>
            <% end %>
            <% (5 - review.stars).times do  %>
              <span class="far fa-star"></span>
            <% end %>
            <p><%= review.content%></p>
          </li>
        </div>
      <% end %>
    <% end %>
  </ul>

<div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>

